alias ls="ls -G"
alias ll="ls -FGlahs"
alias tailf="tail -f"
alias grep="grep --color=always"
alias ip="curl ipecho.net/plain ; echo"

if [ -x "$(command -v colorls)" ]; then
    alias ls="colorls"
    alias la="colorls -al"
fi